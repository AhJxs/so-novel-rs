/downloads
/target
/dist
*.lock.bak

# 根目录运行时文件（首次启动生成；仅本地使用）
/config.toml
/sonovel.db
/sonovel.db-journal
/sonovel.db-wal
/sonovel.db-shm

# 运行时生成的 SQLite DB（下载任务持久化），仅本地使用
/bundle/*.db
/bundle/*.db-journal
/bundle/*.db-wal
/bundle/*.db-shm

# 本地工具状态，不入库
/.claude/
/.codegraph/
/tasks/

# OS / IDE 临时文件（macOS / Windows / VSCode / IntelliJ / 任意编辑器）
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
.githooks.local
